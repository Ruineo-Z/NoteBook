```
[用户 Query]
     │
     ▼
[轻量意图识别模型]
     │
     └─> 输出边类型列表（Edge Type List）
           例如：["参与战役", "引用章节"]
     │
     ▼
[TopK Chunk 向量检索]  ← 基础 RAG
     │
     ▼
[Chunk Metadata]  ← 检索到的 TopK chunk
     │
     └─> 读取 chunk 中涉及的实体节点
           例如：实体 "张飞"
     │
     ▼
[知识图谱拓展]  ← 动态沿边类型查找相关节点
     │
     └─> 根据边类型列表，获取关联节点
           例如：
             张飞 --参与战役--> 赤壁之战
             张飞 --引用章节--> Doc B Chapter 3
     │
     ▼
[关联节点对应的 Chunk]
     │
     ▼
[候选 Chunk 合并]  ← 原 TopK chunk + 图拓展 Chunk
     │
     ▼
[Rerank]  ← 可结合向量相似度 + metadata
     │
     ▼
[上下文构建]  ← 拼接 Parent / Child chunk，控制 token
     │
     ▼
[LLM / Agent 生成答案]

```